on:
  push:
    branches:
      - master

name: 🚀 Deploy Laravel Project on push

jobs:
  laravel-deploy:
    name: 🎉 Deploy Laravel Project
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2.3.2

    - name: 🏗️ Set Permissions
      run: |
        sudo mkdir -p /home/runner/work/github_actions_deploy/github_actions_deploy/storage/logs
        sudo chmod -R 775 /home/runner/work/github_actions_deploy/github_actions_deploy/storage/logs

    - name: ⚙️ Set Laravel Environment
      run: cp .env.example .env

    - name: 🚀 Deploy Laravel Project
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: ftp.topbusiness.io
        username: u463118956.backend
        password: Hyaadodo@1010
        server-dir: /
