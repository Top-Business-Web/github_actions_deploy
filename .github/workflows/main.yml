on:
  push:
    branches:
      - master

name: 🚀 Deploy Laravel Project on push

jobs:
  laravel-deploy:
    name: 🎉 Deploy Laravel Project
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2.3.2

    - name: Check and Set Ownership
      run: |
        sudo chown -R runner:runner /home/runner/work/github_actions_deploy/github_actions_deploy/storage/logsdo chmod -R 775 /home/runner/work/github_actions_deploy/github_actions_deploy/storage/logs

    - name: ⚙️ Set Laravel Environment
      run: cp .env.example .env

    - name: 🚀 Deploy Laravel Project
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        server: ftp.topbusiness.io
        username: u463118956.backend
        password: Hyaadodo@1010
        server-dir: /
